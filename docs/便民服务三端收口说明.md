# 便民服务三端收口说明

## 范围

本次收口以 `markdown_exports` 中的《3-4管理后台操作手册》《3-4小程序操作手册》为基线，对当前 `demo` 做三端对照和补齐，严格区分：

- 便民服务派单：保留在本期 `demo`
- 商城交易 / 商城派单：独立需求，不混入本期

## 已完成的对齐方向

### C 端小程序

- 保留便民服务订单闭环，只展示便民服务订单
- 价格异议、确认支付、联系服务人员、评价等流程保持在便民服务范围内
- 文化院落、购在古城、附近、酒吧 / 餐饮 / 住宿等信息页按信息维护口径保留

### B 端便民服务人员端

- 仅承接便民服务履约
- 修正 `A35`、`A40`、`S48` 的状态含义与交接顺序
- 报价后先到“已收款”，再进入“开始服务”
- 完工拍照、支付凭证、历史订单保持可用

### Desktop 管理后台

- 便民服务概览、派单列表、片区管理、派单配置、服务人员、价格仲裁保留
- 便民服务派单和商城派单完全分离
- 价格争议已接入后台仲裁路径

## 本次修正的关键点

1. `A35` 明确为“已核价 / 待用户支付”
2. `A40` 只表示“已收款”，不再直接跳到服务中
3. `S48` 由 B 端“开始服务”推进
4. `S90`、`A10`、`S10` 的待处理统计已纳入后台
5. 价格异议不再只是提示，而是会落入订单状态链路

## 编译结果

- 前端已成功编译
- 编译输出目录：`/Users/lzz/Downloads/丽江古城游/demo/dist/`
- 入口文件：`/Users/lzz/Downloads/丽江古城游/demo/dist/index.html`

## 备注

- 如需继续细查，可直接按“C 端下单 -> Desktop 派单 -> B 端履约 -> C 端确认/评价”顺序逐页复测。
- 商城交易、商城派单、SKU 派单、商品核销等不在本次范围内。
